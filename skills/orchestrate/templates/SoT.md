# <项目> · SoT — source of truth (read first)

> **What** — the compass: where the project stands, in **pointers, not detail**.
> **Who** — the CEO writes + curates it (authoring.md-grade); small enough to load each session.
> **Rule** — only **Goal** + **Now** are written as text; everything else points to the file that owns the detail.

## Goal
<project-level — what we're building + the bar for "done" (1–2 lines)>

## Now
<what's live · blocked · next (2–3 lines)>

## Open work
→ `docs/TaskBoard.md`

## History
→ `docs/BACKLOG.md`  (finished-task log · append-only · **on-demand** — don't load unless tracing back)

## Spec
→ `docs/<其领域>/SPEC.md`  (if the project has one)

## Decisions
<the few that still shape the project — name the decision + its date; grep that date in `DECISIONS.md` to reach the `## <date> · …` entry (no fragile #anchors)>
- <decision title> → `DECISIONS.md` (<date>)

## Canonical answers & key decisions
→ `docs/CANON.md`  (machine-maintained — current file **and** in-force key decision per question; **read-first**, don't restate its rows here)